hazelcast:
#  properties:
#    hazelcast.jmx: true
  cluster-name: hz-benchmark
  security:
    enabled: false

  map:
    test:
      in-memory-format: OBJECT
      cache-deserialized-values: ALWAYS

  network:
    join:
      multicast:
        enabled: true
